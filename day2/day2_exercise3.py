# store the word in a variable
word = "level"

# check if th word is a palindrome by comparing it to its reverse
is_palindrome = word == word[::-1]

# print the result
print(f"Is {word} a palindrome? {is_palindrome}")
