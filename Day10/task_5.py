# 1. Java habits I had to unlearn or adjust
#
# OOP (Master Class 4):
# In Java, I am used to defining interfaces or abstract classes first
# and relying heavily on explicit inheritance. In Python, I had to adjust
# to duck typing and polymorphism, where objects can be used based on the
# methods they provide without necessarily sharing a parent class.
#
# FastAPI / Testing (Master Class 5):
# In Java, I am used to more explicit type declarations and validation
# through DTOs and annotations. With FastAPI, I had to adjust to using
# Pydantic models for request validation and Python type hints, which
# automatically provide validation and API documentation.
#
# Pandas / NumPy (Master Class 6):
# In Java, I would normally use loops to process elements one by one.
# With NumPy and Pandas, I had to adjust to vectorized operations,
# DataFrame filtering, groupby(), and aggregation instead of manually
# iterating through every record.


# 2. Skill I expect to use again
#
# One skill I expect to use frequently is vectorized NumPy operations.
# In upcoming ML and DL modules, NumPy arrays will be used to represent
# numerical data, features, matrices, and model inputs. Vectorized
# operations will allow me to perform calculations efficiently without
# writing unnecessary Python loops.


# 3. Least confident topic and action plan
#
# The topic I feel least confident about is Python OOP concepts such as
# abstract base classes, duck typing, and understanding when to use
# inheritance versus composition. I will strengthen this by implementing
# a small example using inheritance, polymorphism, duck typing, and ABCs
# and then practicing explaining the differences without referring to
# my notes before the readiness assessment.